# Client Build Context Pack

Prototype for a structured per-client build context object that Caesar and Claude can retrieve before client work.

The pack keeps append-only notes useful, but turns the current state into stable fields: architecture, field maps, formulas, decisions, risks, access state, source links and next actions.
